#include <stdio.h>
#include "lists.h"

/**
 * _strlen - Entry point.
 *
 * Description: Prints out the number of
 * characters on a string
 *
 * @s: String
 *
 * Return: Void
 *
 */

int _strlen(char *s)
{
	int len = 0;

	while ( len[s] != '\0')
	{
		len++;
	}
	return (len);
}
