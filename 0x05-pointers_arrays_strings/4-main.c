#include "main.h"

/**
 * main - checks code.
 *
 * Return: Always 0 on success.
 */
int main(void)
{
	char *str;
	
	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
