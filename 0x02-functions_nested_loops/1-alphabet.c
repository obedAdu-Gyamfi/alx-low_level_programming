#include "main.h"
/**
 * main- Entry point.
 *
 * Return: Always 0 on success.
 *
 */
int main(void)
{
	int alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar(alpha);
	}
	_putchar('\n');
	return (0);
}
