#include<stdio.h>
/**
 * main - Entry point
 * Description: 'Prints out exactly the variable value'
 * Return: Always 0 (succes)
 */ 
int main(void)
{
char my_var[] = "\"Programming is like building a multilingual puzzle";

puts(my_var);
return (0);
}
